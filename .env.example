# Neon PostgreSQL Environment Variables
# 这些变量会在 Vercel 部署时自动配置，本地开发需要手动设置

# Neon 数据库连接 URL（从 Vercel Storage -> Neon 页面复制）
DATABASE_URL="postgresql://username:password@hostname/neondb?sslmode=require"

# 可选：无连接池的连接 URL（用于需要直接连接的场景）
DATABASE_URL_UNPOOLED="postgresql://username:password@hostname/neondb?sslmode=require"
